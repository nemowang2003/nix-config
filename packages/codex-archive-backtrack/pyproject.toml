[project]
name = "codex-archive-backtrack"
version = "0.1.0"
description = "Archive Codex parent threads after backtracking"
requires-python = ">=3.14,<3.15"
dependencies = []

[project.scripts]
codex-archive-backtrack = "codex_archive_backtrack.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
